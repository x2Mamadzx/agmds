{
  "buildCommand": "pnpm --filter @workspace/mds-marketing run build && pnpm --filter @workspace/api-server run build",
  "outputDirectory": "artifacts/mds-marketing/dist/public",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
